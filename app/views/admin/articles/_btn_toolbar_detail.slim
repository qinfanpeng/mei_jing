
.form-actions-admin
  = link_to t("helpers.links.back"), :back, :class => 'btn btn-mini'
  = link_to t("helpers.links.edit"), edit_admin_article_path(article), :class => 'btn btn-mini'
  = button_to t("helpers.links.destroy"), admin_article_path(article), :method => "delete", data: { confirm: t("helpers.links.confirm") }, class: 'btn btn-danger btn-mini'
  = button_to t('article.draft'), draft_admin_article_path(article), class: 'btn btn-mini'
  = button_to t('article.ban'), ban_admin_article_path, class: 'btn btn-mini btn-warning'

  = form_tag classify_admin_article_path(article), class: 'classify_form' do 
    .btn-group
      button.btn.btn-mini.dropdown-toggle data-toggle= "dropdown"
        = t('article.classify')
        span.caret
      ul.dropdown-menu.dropdown-menu-admin
        - columns.each do |column|
          li
            a
              - checked = article.columns.include?(column)
              = check_box_tag('column_ids[]', column.id, checked)
              = column.name
        li.divider
        li= link_to t('helpers.ok'), '', class: 'classify_ok'
  
  = button_to t('article.publish'), publish_admin_article_path, class: 'btn btn-mini btn-success'
  = link_to t('article.preview'), article_path(article), class: 'btn btn-mini'
